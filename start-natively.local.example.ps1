# Copy this file to start-natively.local.ps1 and set local-only overrides.
# The real start-natively.local.ps1 is ignored by git.

# Example:
# $env:NATIVELY_STT_PROVIDER = 'deepgram'
# $env:DEEPGRAM_API_KEY = 'your-deepgram-api-key'
